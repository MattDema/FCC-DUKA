#cloud-config
package_update: true
package_upgrade: false

# ============================================
# 1. PREREQUISITI SISTEMA
# ============================================
write_files:
  # Abilitare IP forwarding e bridge (requisito K8s)
  - path: /etc/sysctl.d/k8s.conf
    content: |
      net.bridge.bridge-nf-call-iptables = 1
      net.bridge.bridge-nf-call-ip6tables = 1
      net.ipv4.ip_forward = 1

  # Moduli kernel necessari
  - path: /etc/modules-load.d/k8s.conf
    content: |
      overlay
      br_netfilter

  # Configurazione containerd con SystemdCgroup
  - path: /etc/containerd/config.toml
    content: |
      version = 2
      [plugins."io.containerd.grpc.v1.cri"]
        sandbox_image = "registry.k8s.io/pause:3.9"
        [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc]
          runtime_type = "io.containerd.runc.v2"
          [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc.options]
            SystemdCgroup = true

runcmd:
  # ============================================
  # 2. MODULI KERNEL
  # ============================================
  - modprobe overlay
  - modprobe br_netfilter
  - sysctl --system

  # ============================================
  # 3. INSTALLARE CONTAINERD
  # ============================================
  - apt-get update
  - apt-get install -y containerd apt-transport-https ca-certificates curl gnupg
  - mkdir -p /etc/containerd
  # (config.toml già scritto sopra da write_files)
  - systemctl restart containerd
  - systemctl enable containerd

  # ============================================
  # 4. INSTALLARE KUBEADM, KUBELET, KUBECTL
  # ============================================
  - mkdir -p /etc/apt/keyrings
  - curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.29/deb/Release.key | gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
  - echo 'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.29/deb/ /' > /etc/apt/sources.list.d/kubernetes.list
  - apt-get update
  - apt-get install -y kubelet=1.29.* kubeadm=1.29.* kubectl=1.29.*
  - apt-mark hold kubelet kubeadm kubectl

  # ============================================
  # 5. DISABILITARE SWAP (requisito K8s)
  # ============================================
  - swapoff -a
  - sed -i '/swap/d' /etc/fstab

  # ============================================
  # 6. INIZIALIZZARE IL CLUSTER
  # ============================================
  - kubeadm init --pod-network-cidr=10.244.0.0/16 --token-ttl=0 | tee /root/kubeadm-init.log

  # Configurare kubectl per root
  - mkdir -p /root/.kube
  - cp /etc/kubernetes/admin.conf /root/.kube/config

  # Configurare kubectl per l'utente ubuntu/default
  - mkdir -p /home/ubuntu/.kube
  - cp /etc/kubernetes/admin.conf /home/ubuntu/.kube/config
  - chown -R 1000:1000 /home/ubuntu/.kube

  # ============================================
  # 7. INSTALLARE FLANNEL (CNI - rete tra pod)
  # ============================================
  - kubectl --kubeconfig=/etc/kubernetes/admin.conf apply -f https://github.com/flannel-io/flannel/releases/latest/download/kube-flannel.yml

  # ============================================
  # 8. SALVARE IL JOIN COMMAND
  # ============================================
  - kubeadm token create --print-join-command > /root/join-command.txt
  - chmod 644 /root/join-command.txt
